#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../embedly/bin/embedly_generic.js" "$@"
  ret=$?
else 
  node  "$basedir/../embedly/bin/embedly_generic.js" "$@"
  ret=$?
fi
exit $ret
